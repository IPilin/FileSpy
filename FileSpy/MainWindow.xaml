<Window x:Class="FileSpy.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FileSpy"
        xmlns:elements="clr-namespace:FileSpy.Elements"
        mc:Ignorable="d"
        Title="FileSpy" Height="300" Width="510" WindowStyle="None" Background="Transparent" AllowsTransparency="True" 
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Loaded="Window_Loaded" KeyUp="Window_KeyUp">
    <Window.Resources>
        <SolidColorBrush x:Key="ListBox.Static.Background" Color="#FFFFFFFF"/>
        <SolidColorBrush x:Key="ListBox.Static.Border" Color="#FFABADB3"/>
        <SolidColorBrush x:Key="ListBox.Disabled.Background" Color="#FFFFFFFF"/>
        <SolidColorBrush x:Key="ListBox.Disabled.Border" Color="#FFD9D9D9"/>
        <Style x:Key="ListBoxStyle1" TargetType="{x:Type ListBox}">
            <Setter Property="Background" Value="{StaticResource ListBox.Static.Background}"/>
            <Setter Property="BorderBrush" Value="{StaticResource ListBox.Static.Border}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.CanContentScroll" Value="true"/>
            <Setter Property="ScrollViewer.PanningMode" Value="Both"/>
            <Setter Property="Stylus.IsFlicksEnabled" Value="False"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ListBox}">
                        <Border x:Name="Bd" BorderThickness="0" Background="Transparent" Padding="1" SnapsToDevicePixels="true">
                            <ScrollViewer Focusable="false" Padding="{TemplateBinding Padding}">
                                <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                            </ScrollViewer>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Background" TargetName="Bd" Value="{StaticResource ListBox.Disabled.Background}"/>
                                <Setter Property="BorderBrush" TargetName="Bd" Value="{StaticResource ListBox.Disabled.Border}"/>
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsGrouping" Value="true"/>
                                    <Condition Property="VirtualizingPanel.IsVirtualizingWhenGrouping" Value="false"/>
                                </MultiTrigger.Conditions>
                                <Setter Property="ScrollViewer.CanContentScroll" Value="false"/>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>
    <Window.Effect>
        <DropShadowEffect Color="Gray" BlurRadius="10" Opacity="0.5" Direction="-90" RenderingBias="Quality" ShadowDepth="4"/>
    </Window.Effect>
    <Grid x:Name="Grid" Margin="255,10,255,10" Opacity="1">
        <Border Background="#92B9C0" CornerRadius="10,10,0,0" Opacity="0.7" Height="40" VerticalAlignment="Top">
        </Border>
        <Border VerticalAlignment="Top" Height="50" CornerRadius="10,10,0,0">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="Transparent" Offset="0"/>
                    <GradientStop x:Name="GreenColor" Color="Green" Offset="1.4"/>
                    <GradientStop x:Name="RedColor" Color="Red" Offset="0.7"/>
                </LinearGradientBrush>
            </Border.Background>
            <Grid>
                <Grid x:Name="Topper" MouseLeftButtonDown="Topper_MouseLeftButtonDown" Background="White" Opacity="0"></Grid>
                <Ellipse x:Name="CloseButton" Cursor="Hand" MouseLeftButtonUp="CloseButton_MouseLeftButtonUp" Margin="0,7,10,0" Fill="Red" Stroke="Black" StrokeThickness="0" Width="15" Height="15" HorizontalAlignment="Right" VerticalAlignment="Top">
                    <Ellipse.Effect>
                        <DropShadowEffect BlurRadius="2" Opacity="0.25" ShadowDepth="1"/>
                    </Ellipse.Effect>
                </Ellipse>
                <Ellipse x:Name="MinimazeButton" Cursor="Hand" MouseLeftButtonUp="MinimazeButton_MouseLeftButtonUp" Margin="0,7,30,0" Fill="YellowGreen" Stroke="Black" StrokeThickness="0" Width="15" Height="15" HorizontalAlignment="Right" VerticalAlignment="Top">
                    <Ellipse.Effect>
                        <DropShadowEffect BlurRadius="2" Opacity="0.25" ShadowDepth="1"/>
                    </Ellipse.Effect>
                </Ellipse>
            </Grid>
        </Border>
        <Border x:Name="Table" Margin="0,30,0,0" CornerRadius="10" Background="#FFFEFF">
            <Grid>
                <Grid Height="60" VerticalAlignment="Top">
                    <elements:SwitchControl Margin="10,0,0,0" HorizontalAlignment="Left"/>
                    <Border x:Name="SettingsButton" Cursor="Hand" MouseLeftButtonUp="SettingsButton_MouseLeftButtonUp"  Margin="0,0,10,0" HorizontalAlignment="Right" Height="30" Width="80" CornerRadius="15">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                <GradientStop Color="#579EE1" Offset="0"/>
                                <GradientStop Color="#127FE8" Offset="0.5"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="4" Opacity="0.25" ShadowDepth="2"/>
                        </Border.Effect>
                        <Label FontSize="14" FontFamily="./Resources/#Ubuntu" Foreground="#FFFEFF" VerticalAlignment="Center" HorizontalAlignment="Center">Settings</Label>
                    </Border>
                </Grid>
                <Grid Margin="0,60,0,20">
                    <ListBox x:Name="FullTable" VirtualizingPanel.ScrollUnit="Pixel" ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Disabled" SelectionMode="Single" Style="{DynamicResource ListBoxStyle1}">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="{x:Type ListBoxItem}">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="HorizontalContentAlignment" Value="{Binding HorizontalContentAlignment, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"/>
                                <Setter Property="VerticalContentAlignment" Value="{Binding VerticalContentAlignment, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"/>
                                <Setter Property="Padding" Value="2,0,0,0"/>
                                <Setter Property="FocusVisualStyle">
                                    <Setter.Value>
                                        <Style>
                                            <!-- This removes focus visualization -->
                                            <Setter Property="Control.Template" Value="{x:Null}"/>
                                        </Style>
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="{x:Type ListBoxItem}">
                                            <Border x:Name="Bd" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="true">
                                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsEnabled" Value="false">
                                                    <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"/>
                                                </Trigger>
                                                <!-- Some default triggers removed to avoid background changes on selection -->
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                    </ListBox>
                    <Label x:Name="DisconnectLabel" Foreground="Red" Opacity="1" VerticalAlignment="Center" HorizontalAlignment="Center" FontFamily="./Resources/#Ubuntu" FontSize="13">Connection failed.</Label>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>
